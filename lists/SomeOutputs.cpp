#include "SomeOutputs.h"
#include <bits/stdc++.h>

/*std::string SomeOutputs::beforeSymbol(std::string line, std::string symbol) {
    char chars[line.size() + 1];
    std::strcpy(chars, line.c_str());
    for (int i = 0; i < )
}*/